[project]
name = "kairos-ci-tools"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "jsonschema==4.23.0",
  "PyYAML==6.0.2",
]

[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"
